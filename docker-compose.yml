version: '3.8'

services:
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=sofia_health
      - POSTGRES_USER=sofia_user
      - POSTGRES_PASSWORD=sofia_pass
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sofia_user"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build: .
    command: gunicorn sofia_health.wsgi:application --bind 0.0.0.0:8000 --reload
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      - DEBUG=True
      - SECRET_KEY=django-insecure-dev-key-only-for-local-development
      - DATABASE_URL=postgresql://sofia_user:sofia_pass@db:5432/sofia_health
      - STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY:-pk_test_51IajQRSBK5wSWQxVZng9VBJJTbdPLFRzOtpOFHRLe7VKZzOmWNmDXfCYpuDO5zfLqQzFe3HoQjzUYPw8ZYSB8vRP00wb59j2vl}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY:-sk_test_51IajQRSBK5wSWQxVXD8voTIW2DjwYPPlbOhVoeNelyYv1I6PYHxI9YgJmyU0sHwIFRzfCAHSKxMXG0HPQ9I4JXQS00B5KdQyRn}
      - ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

volumes:
  postgres_data: